{
  "name": "ElCalendar",
  "props": {
    ":v-model": {
      "value": "",
      "description": "Binding value",
      "default": "—",
      "type": "Date",
      "description_zh": "绑定值",
      "$model": true
    },
    ":range": {
      "value": "",
      "description": "Time range, including start time and end time. Start time must be start day of week, end time must be end day of week, the time span cannot exceed two months",
      "default": "—",
      "type": "[Date, Date]",
      "description_zh": "时间范围，包括开始时间与结束时间。开始时间必须是周一，结束时间必须是周日，且时间跨度不能超过两个月。"
    }
  },
  "events": [],
  "slots": [
    {
      "name": "date-cell",
      "description": "Content of date cell. `type` indicates which month the date belongs, optional values are prev-month, current-month, next-month; `isSelected` indicates whether the date is selected; `day` is the formatted date in the format `YYYY-MM-DD`; `date` is date the cell represents",
      "description_zh": "单元格内容。`type` 表示该日期的所属月份，可选值有 prev-month，current-month，next-month；`isSelected` 标明该日期是否被选中；`day` 是格式化的日期，格式为 YYYY-MM-DD；`date` 是单元格代表的日期",
      "params": "{ data: { type: 'prev-month' | 'current-month' | 'next-month', isSelected: boolean, day: string, date: Date } }"
    },
    {
      "name": "header",
      "description": "Content of the Calendar header",
      "description_zh": "日历头部内容",
      "params": "{ date: string }"
    }
  ],
  "exposed": [
    {
      "name": "selectedDay",
      "description": "Currently selected date",
      "description_zh": "当前选中的日期",
      "detail": "ComputedRef<Dayjs | undefined>"
    },
    {
      "name": "pickDay",
      "description": "Select a specific date",
      "description_zh": "选择特定日期",
      "detail": "(day: dayjs.Dayjs) => void"
    },
    {
      "name": "selectDate",
      "description": "Select date",
      "description_zh": "选择日期",
      "detail": "(type: CalendarDateType) => void"
    },
    {
      "name": "calculateValidatedDateRange",
      "description": "Calculate the validate date range according to the start and end dates",
      "description_zh": "根据开始和结束日期计算有效的日期范围",
      "detail": "(startDayjs: dayjs.Dayjs, endDayjs: dayjs.Dayjs) => [dayjs.Dayjs, dayjs.Dayjs][]"
    }
  ],
  "typeDetail": {
    "CalendarDateType": "'prev-month' | 'next-month' | 'prev-year' | 'next-year' | 'today'"
  },
  "link": "https://element-plus.org/en-US/component/calendar.html",
  "link_zh": "https://element-plus.org/zh-CN/component/calendar.html",
  "methods": []
}
